# Archiv — ältere, nicht genutzte Scripts

Alles hier ist **nicht Teil des aktuellen Setups** und wird nicht gepflegt. Es liegt nur
deshalb im Repo, weil es sonst auf verstreuten Branches verloren gegangen wäre — die sind
inzwischen gelöscht.

Das aktive Setup sind die zwei Indikatoren eine Ebene höher: `indicator_overlay_simple.pine`
(Slot 1) und `indicator_lower_macd.pine` (Slot 2). Siehe `../CLAUDE.md`.

| Datei | Was es war | Herkunft |
|---|---|---|
| `BreakoutVolume.pine` | Ausbruch + Volumenbestätigung, alternatives Konzept | Branch `crypto-intraday-indicator-kskbc4` |
| `ShortPullback.pine` | Short-only Intraday-Pullback v1 | dito |
| `SmartMoneyIntraday.pine` | Smart-Money-Skelett, nur Layer 1 (Regime) | dito |
| `RegimeAdaptiveIntraday.pine` | Regime-adaptive Intraday-Strategie | dito |
| `crypto_trend_continuation_strategy.pine` | Trendfortsetzungs-Backtest | Branch `tradingview-crypto-strategy-wbu1q6` |

Alle Pine v6, alle mit Signalen — also nach dem Leitprinzip des aktuellen Setups ausdrücklich
nicht das, was gebaut werden soll. Vor einer Wiederverwendung neu prüfen: keins davon ist
kompiliert oder gebacktestet worden.
